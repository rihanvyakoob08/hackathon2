# KrishiMitra Frontend

React frontend for the FastAPI backend.

## Run

```powershell
cd frontend
npm install
npm run dev
```

The app expects the backend at `http://localhost:8000`. Override with `VITE_API_BASE_URL` in `.env`.
